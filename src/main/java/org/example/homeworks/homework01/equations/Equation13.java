package org.example.homeworks.homework01.equations;

import static java.lang.Math.*;

public class Equation13 {
    public static void main(String[] args) {

        double x = 0;

        double y = pow(pow(E, -2 + x), 1. / 5) * (1. / sqrt(x * x + pow(x, 4) + log(abs(x - 3.14))));
    }
}
