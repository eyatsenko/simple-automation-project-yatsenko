package org.example.homeworks.homework01.equations;

import static java.lang.Math.*;

public class Equation21 {
    public static void main(String[] args) {

        double x = 0;

        double y = (pow(log10(x), 1. / 4) + acos(x + 3)) * (1 / (abs(x + 2 * pow(x, 2))));
    }
}
