package org.example.homeworks.homework01.equations;

import static java.lang.Math.*;

public class Equation11 {
    public static void main(String[] args) {

        double x = 0;

        double y = 2. * PI * x - abs(sin(sqrt(10.5 * x))) * (1 / (pow(pow(x, 2), 1. / 3) + 1. / 7));
    }
}
