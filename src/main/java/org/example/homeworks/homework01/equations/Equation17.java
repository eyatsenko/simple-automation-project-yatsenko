package org.example.homeworks.homework01.equations;

import static java.lang.Math.*;

public class Equation17 {
    public static void main(String[] args) {

        double x = 0;

        double y = pow(x, 3) / 3 - pow(E, x) * log(abs(pow(1.3, 3) + pow(x, 3))) + 4. / 3;
    }
}
