package org.example.homeworks.homework01.equations;

import static java.lang.Math.*;

public class Equation12 {
    public static void main(String[] args) {

        double x = 0;

        double y = log(sqrt(abs(2 - x) + 1.2)) * (1 / (2 + pow(E, -x))) + pow(2 / x, 1. / 3);
    }
}
