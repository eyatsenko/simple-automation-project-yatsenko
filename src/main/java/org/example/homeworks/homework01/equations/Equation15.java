package org.example.homeworks.homework01.equations;

import static java.lang.Math.*;

public class Equation15 {
    public static void main(String[] args) {

        double x = 0;

        double y = (abs(x * log(x) - 4) * sqrt(x)) * (1 / (pow(pow(E, 4 * x - 1), 1. / 5)));
    }
}